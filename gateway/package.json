{
  "name": "@recluster/gateway",
  "version": "0.0.1",
  "description": "reCluster gateway",
  "author": "Carlo Corradini <carlo.corradini98@gmail.com>",
  "license": "MIT",
  "private": true,
  "scripts": {
    "start": "build/app.js",
    "watch": "npx ts-node-dev --respawn --clear --transpile-only --require tsconfig-paths/register src/app.ts"
  },
  "dependencies": {
    "@apollo/gateway": "^2.0.0-preview.9",
    "apollo-server": "^3.6.7",
    "graphql": "^16.3.0"
  }
}
